#include<stdio.h>
int fact(int x) {
if(x == 0 || x == 1) 
  return 1;
else 
  return (x * fact(x - 1));
  }

int main() {
  int y,f,x;
   y = 5;
  f = fact(y);
  printf("The factorial of %d is : %d ",y,f);
  return 0;

  
  
}
